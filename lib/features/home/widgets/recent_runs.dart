import 'package:flutter/material.dart';

class RecentRuns extends StatelessWidget {
  const RecentRuns({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Recent Runs'));
  }
}